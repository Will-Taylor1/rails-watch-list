<h1>List of lists of movies</h1>

<ul>
  <% @lists.each do |list| %>
    <li>
    <%= link_to list.name, list_path(list) %>
    </li>
    <% end %>
    <%= link_to "Add a new list", new_list_path, class: "btn btn-primary" %>
</ul>

